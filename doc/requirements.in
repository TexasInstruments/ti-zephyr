# Copyright (c) 2026 Texas Instruments Incorporated
#
# SPDX-License-Identifier: BSD-3-Clause
#
# DOC: used to generate docs. Trimmed from upstream Zephyr's doc/requirements.in
# — dropped sphinx-sitemap (no publish/sitemap use case yet) and
# sphinxcontrib-svg2pdfconverter (LaTeX/PDF build not wired up in this
# minimal framework pass).

sphinx<8.3.0
sphinx_rtd_theme~=3.0
sphinx-tabs
pygments>=2.9,!=2.19.0
sphinx-notfound-page
sphinx-copybutton
sphinx-togglebutton
sphinx-autobuild
sphinxcontrib.programoutput

# YAML validation. Used by zephyr_module.
PyYAML>=6.0
pykwalify

# Used by pytest-twister-harness plugin (imported by autodoc)
pytest
pyserial

# Doxygen doxmlparser
doxmlparser

# Used by the Zephyr domain to organize code samples
anytree

# Used to generate API coverage reports
coverxygen

# Used by Kconfig extension
python-dotenv
